import React from "react";
import { Grid, Box } from '@mui/material';

type TileAddedEshopProps = {

}

const TileAddedEshop: React.FC<TileAddedEshopProps> = ({ }) => {
    return (
        <React.Fragment>
            <Box sx={{ border: '1px solid #ddd', padding: 2, height: '100%' }}>
                {/* First tile content */}
                TileAddedEshop
            </Box>
        </React.Fragment>
    )
}
export default TileAddedEshop;